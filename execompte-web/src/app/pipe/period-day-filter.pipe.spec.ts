import { PeriodDayFilterPipe } from './period-day-filter.pipe';

describe('PeriodDayFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PeriodDayFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
